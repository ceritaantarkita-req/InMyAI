.env
.env.*
!.env.example
node_modules/
.next/
__pycache__/
.pytest_cache/
.venv/
*.pyc
*.sqlite
*.db
*.gguf
*.safetensors
*.ckpt
*.bin
models/*
!models/registry.json
!models/registry.example.json
!models/.gitkeep
downloads/
workspace/
data/runtime/
data/*.sqlite
uploads/
generated/
artifacts/
graphify-out/
.DS_Store
Thumbs.db

# Test runtime isolation (per-session temp DB + workspace)
.test-runtime/

# Tauri desktop shell build output (Rust compile artifacts, Next.js static
# export used only for the packaged production build, and gen/schemas -
# capability/ACL JSON schemas auto-regenerated on every build, per Tauri's
# own project template). Cargo.lock IS committed on purpose - this is an
# application, not a library, so a locked dependency tree is wanted.
apps/web/src-tauri/target/
apps/web/src-tauri/gen/schemas/
apps/web/out/
